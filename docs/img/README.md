# Screenshots

Drop the following in this directory and they will appear in the project README.

| File | What to capture |
|---|---|
| `booking-assistant.png` | `/book` after asking for "a haircut next Tuesday afternoon" — the parsed intent and the real slots below it |
| `dashboard.png` | `/admin` with the AI briefing, today's diary and the "worth a phone call" panel |
| `risk-detail.png` | The risk modal open on a high-risk booking, showing the factor breakdown |
| `availability-editor.png` | `/admin/team/1/hours` with the timezone note and a split-shift day |
| `ai-usage.png` | `/admin/ai` — calls, spend against budget, and the recent-calls list |
| `booking-mobile.png` | `/book` at 375px |

Capture at 1440px wide unless noted, in both light and dark if you want a pair.
